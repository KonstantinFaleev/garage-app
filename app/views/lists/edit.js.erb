$('#list_<%= @list[:id] %> .row').hide().parent().append("<%= j render 'edit' %>");
$('#list_<%= @list[:id] %> .todo_items_wrapper').hide().parent();
$('#list_<%= @list[:id] %> .todo_list_title').first().hide().parent();
$('#list_<%= @list[:id] %>')[0].scrollIntoView({
    behavior: 'auto',
    block: 'center',
    inline: 'center'
});
